// app/login/page.tsx

import LoginForm from '../../../components/login-form'; // Assuming login-form.tsx is in the app directory

const LoginPage = () => {
    return (
        <div>
            <h1>Login Page</h1>
            <LoginForm />
        </div>
    );
};

export default LoginPage;
